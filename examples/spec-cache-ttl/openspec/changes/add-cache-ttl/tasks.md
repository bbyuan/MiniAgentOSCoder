# Tasks

- [ ] Extend `CacheEntry` with optional expiration metadata.
- [ ] Validate negative TTL values.
- [ ] Evict expired entries from `get`.
- [ ] Implement `clear_expired`.
- [ ] Run `python3 -m unittest discover -v`.
