# Cache TTL Spec Fixture

- Read `openspec/project.md` first.
- Implement the active change under `openspec/changes/add-cache-ttl/`.
- Keep the public `SimpleCache` API backwards compatible.
- Run `python3 -m unittest discover -v` before finishing.
