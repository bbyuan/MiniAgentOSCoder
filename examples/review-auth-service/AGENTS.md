# Auth Service Review Fixture

- Review mode should stay read-only.
- Prioritize security, correctness, privacy, and test gaps.
- Do not rewrite the module unless the user explicitly switches to Bugfix or Feature mode.
- Run `python3 -m unittest discover -v` only as supporting evidence if needed.
