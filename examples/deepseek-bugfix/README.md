# DeepSeek Bugfix Demo

This intentionally broken Python project exercises the complete MiniAgentOS Coder flow: workspace scanning, context selection, planning, guarded file edits, test execution, and trace reporting.

Run the baseline test to confirm the fixture starts in a failing state:

```bash
pytest
```

Use the task in `TASK.md` when starting a run from the workbench.
