# Contact Book Feature Fixture

- Use only the Python standard library.
- Keep contact identity based on normalized email addresses.
- Preserve insertion order for exported contacts.
- Run `python3 -m unittest discover -v` before finishing.
