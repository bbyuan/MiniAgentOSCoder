# Invoice Rules Fixture

- Use integer cents for totals.
- Read `docs/tax_rules.md` before changing tax behavior.
- Prefer one focused patch in `invoice.py`.
- Run `python3 -m unittest discover -v` before finishing.
